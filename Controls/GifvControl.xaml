<UserControl
    x:Class="Discord_UWP.Controls.GifvControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Discord_UWP.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400" SizeChanged="UserControl_SizeChanged">

    <Grid x:Name="content" MaxWidth="400">
        <Grid x:Name="LoadingIndic">
            <Grid.Background>
                <SolidColorBrush Opacity="0.2" Color="{ThemeResource SystemAltHighColor}"/>
            </Grid.Background>
            <ProgressRing Foreground="{ThemeResource Blurple}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
        <MediaElement MediaOpened="mediaelement_MediaOpened" MediaEnded="mediaelement_MediaEnded" x:Name="mediaelement" AreTransportControlsEnabled="False" AutoPlay="True" />
    </Grid>
</UserControl>
