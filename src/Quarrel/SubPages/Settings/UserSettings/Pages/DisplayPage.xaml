<UserControl
    x:Class="Quarrel.SubPages.Settings.UserSettings.Pages.DisplayPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:glob="using:System.Globalization"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <ScrollViewer>
        <StackPanel Padding="16">
            <ComboBox SelectedItem="{x:Bind ViewModel.SelectedLanguage, Mode=TwoWay}"
                      ItemsSource="{x:Bind ViewModel.LanguageOptions}">
                <ComboBox.ItemTemplate>
                    <DataTemplate x:DataType="glob:CultureInfo">
                        <TextBlock>
                            <Run Text="{x:Bind NativeName}"/>
                            <Run Text=" - "/>
                            <Run Text="{x:Bind DisplayName}"/>
                        </TextBlock>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>
    </ScrollViewer>
</UserControl>
