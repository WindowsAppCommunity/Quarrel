<UserControl
    x:Class="Quarrel.SubPages.GuildSettings.GuildSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Quarrel.SubPages.GuildSettings"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ctl="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <ctl:NavigationView x:Name="NavigationControl" IsSettingsVisible="False" IsBackButtonVisible="Collapsed"
                        OpenPaneLength="180" CompactModeThresholdWidth="0"
                        SelectionChanged="NavigationView_SelectionChanged">
            <ctl:NavigationView.MenuItems>
                <ctl:NavigationViewItemHeader Content="Server Settings"/>
                <ctl:NavigationViewItem x:Name="OverviewItem" Icon="PreviewLink" Content="Overview"/>
                <ctl:NavigationViewItem x:Name="RolesItem" Icon="ContactPresence" Content="Roles"/>
                <ctl:NavigationViewItem x:Name="EmojisItem" Icon="Emoji" Content="Emojis"/>
                <ctl:NavigationViewItem x:Name="ModerationItem" Icon="ReportHacked" Content="Moderation"/>
                <ctl:NavigationViewItem x:Name="AuditLogItem" Icon="ShowResults" Content="Audit Log"/>


                <ctl:NavigationViewItemHeader Content="User managment"/>
                <ctl:NavigationViewItem x:Name="MembersItem" Icon="People" Content="Members"/>
                <ctl:NavigationViewItem x:Name="InvitesItem" Icon="AddFriend" Content="Invites"/>
                <ctl:NavigationViewItem x:Name="BansItem" Icon="BlockContact" Content="Bans"/>
            </ctl:NavigationView.MenuItems>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="42"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Border>
                    <TextBlock x:Name="HeaderTB" Text="Overview" Margin="16,0,0,0" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="20"/>
                </Border>

                <Frame x:Name="SettingsFrame" Grid.Row="1" Margin="12,8"/>
            </Grid>
        </ctl:NavigationView>
    </Grid>
</UserControl>
