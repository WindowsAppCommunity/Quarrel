<UserControl
    x:Class="Quarrel.Views.MessageView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Quarrel.Views"
    xmlns:messagecontrols="using:Quarrel.Controls.Messages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <ListView ItemsSource="{x:Bind ViewModels.Source, Mode=OneWay}"
              SelectionMode="None" IsItemClickEnabled="False">
        <ListView.ItemsPanel>
            <ItemsPanelTemplate>
                <ItemsStackPanel Margin="0,0,8,0" ItemsUpdatingScrollMode="KeepLastItemInView">
                    <ItemsStackPanel.ChildrenTransitions>
                        <TransitionCollection/>
                    </ItemsStackPanel.ChildrenTransitions>
                </ItemsStackPanel>
            </ItemsPanelTemplate>
        </ListView.ItemsPanel>
        <ListView.ItemTemplate>
            <DataTemplate>
                <messagecontrols:MessageTemplate Margin="0,0,0,8"/>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</UserControl>
