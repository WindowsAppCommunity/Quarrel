<UserControl
    x:Class="Quarrel.Controls.Messages.ActionMessageTemplate"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Quarrel.Controls.Messages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:markdown="using:Quarrel.Controls.Markdown"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Height="32">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="64"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <FontIcon Glyph="" Foreground="{StaticResource online}"/>
        <StackPanel Orientation="Horizontal" Grid.Column="1" VerticalAlignment="Center">
            <TextBlock Text="{x:Bind ViewModel.AuthorName}" FontWeight="Bold"/>
            <TextBlock Text="Joined the Server!" FontWeight="SemiLight" Opacity="0.9" Margin="8,0,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
