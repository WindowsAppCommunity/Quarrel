<UserControl
    x:Class="Quarrel.Controls.Messages.MessageFlyout"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Quarrel.Controls.Messages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel HorizontalAlignment="Stretch" Width="96">
        <MenuFlyoutItem Text="Pin" Visibility="{x:Bind ShowPin}" Width="96" Click="Pin"/>
        <MenuFlyoutItem Text="Unpin" Visibility="{x:Bind ShowUnpin}" Width="96" Click="Pin"/>
        <MenuFlyoutItem Text="Edit" Visibility="{x:Bind ShowEdit}" Width="96" Click="Edit"/>
        <MenuFlyoutItem Text="Copy Id" Width="96"/>
        <MenuFlyoutItem Text="Delete" Visibility="{x:Bind ShowDelete}" Width="96" Click="Delete"/>
    </StackPanel>
</UserControl>
