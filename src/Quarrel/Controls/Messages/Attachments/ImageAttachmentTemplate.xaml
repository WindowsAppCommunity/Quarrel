<UserControl
    x:Class="Quarrel.Controls.Messages.Attachments.ImageAttachmentTemplate"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Quarrel.Controls.Messages.Attachments"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <toolkit:ImageEx Source="{x:Bind ViewModel.Url, Mode=OneWay}"
                         MaxHeight="500" MaxWidth="800"
                         Height="{x:Bind ViewModel.ActualHeight, Mode=OneWay}"
                         Width="{x:Bind ViewModel.ActualWidth, Mode=OneWay}"
                         HorizontalAlignment="Left"/>
    </Grid>
</UserControl>
