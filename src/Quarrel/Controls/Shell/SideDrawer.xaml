<UserControl
    x:Class="Quarrel.Controls.Shell.SideDrawer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Quarrel.Controls.Shell"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:qc="using:Quarrel.Controls"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    SizeChanged="OnSizeChanged">

    <Grid x:Name="rootgrid" Background="White"
          PointerPressed="OnPointerPressed">
        <Grid x:Name="leftwrap" HorizontalAlignment="Left">
            <Grid x:Name="left1">
                
            </Grid>
            <Grid x:Name="left2">
                
            </Grid>
        </Grid>
        <Grid x:Name="right" HorizontalAlignment="Right">
            
        </Grid>
        <Grid x:Name="main" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <qc:Shadow HorizontalAlignment="Left" Direction="Left" Width="12" Margin="-12,0,0,0"/>
            <qc:Shadow HorizontalAlignment="Right" Direction="Right" Width="12" Margin="0,0,-12,0"/>
            <ContentControl x:Name="MainContentControl"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            Margin="0,0,0,0" IsTabStop="False"/>
        </Grid>
    </Grid>
</UserControl>
