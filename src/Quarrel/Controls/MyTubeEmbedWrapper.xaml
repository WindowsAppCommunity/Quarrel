<UserControl
    x:Class="Quarrel.Controls.MyTubeEmbedWrapper"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Quarrel.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="using:Ryken.UI"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <UserControl.Resources>
        <Style TargetType="ui:RykenPlayerControls" x:Key="ControlsStyle">
            <Setter Property="ControlsSize" Value="80" />
            <Setter Property="PlayButtonSize" Value="120" />
            <Setter Property="MiniButtonSize" Value="60" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <ui:RykenMediaContainer x:Name="RykenPlayer" 
                                ShowFullscreenButton="False" 
                                PlayerControlsStyle="{StaticResource ControlsStyle}"
                                />
    </Grid>
</UserControl>
