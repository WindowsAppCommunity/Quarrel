<UserControl
    x:Class="Quarrel.Controls.Panels.Channels.GuildHeader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Quarrel.Controls.Panels.Channels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Height="48"
    d:DesignHeight="40"
    d:DesignWidth="400">

    <Button Background="Transparent"
            BorderThickness="0"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            HorizontalContentAlignment="Stretch"
            Command="{x:Bind ViewModel.OpenGuildSettingsCommand}">
        <TextBlock Text="{x:Bind ViewModel.SelectedGuild.Name, Mode=OneWay}"
                   HorizontalAlignment="Left" FontSize="20"/>
    </Button>
</UserControl>
