<UserControl
    x:Class="Quarrel.Controls.Panels.Messages.VoicePanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Quarrel.Controls.Panels.Messages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:bvoice="using:Quarrel.Bindables.Voice"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Border Background="Black" Opacity=".25" CornerRadius="0,0,8,8">
        <ListView ItemsSource="{x:Bind ViewModel.SelectedChannel.VoiceMembers, Mode=OneWay}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="bvoice:BindableVoiceState">
                    <TextBlock Text="{x:Bind State.User.Id}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Border>
</UserControl>
